:range(a, b) {
  if (a > b) {
    :c = a + 1;
    ->:() {
      c -= 1;
      if (c < b) ->nil;
      ->c;
    }
  } else {
    :c = a - 1;
    ->:() {
      c += 1;
      if (c > b) ->nil;
      ->c;
    }
  }
}

